<?xml version="1.0" encoding="utf-8"?>
<!-- Adaptive-icon foreground: the original mascot bitmap is untouched; it is
     only inset so the artwork sits inside Android's 66% safe zone instead of
     being cropped by the launcher mask (previously it looked zoomed in). -->
<inset xmlns:android="http://schemas.android.com/apk/res/android"
    android:drawable="@mipmap/ic_launcher_foreground"
    android:inset="20%" />
