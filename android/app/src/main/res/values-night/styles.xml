<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!--
      Dark-mode variant of the real app theme. Only the system-bar icon
      appearance changes: dark backgrounds need light (white) icons, so the
      "light bar" flags are false here. No deprecated bar-colour attributes.
    -->
    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:windowBackground">@color/appWindowBackground</item>
        <item name="android:windowLightStatusBar">false</item>
        <item name="android:windowLightNavigationBar">false</item>
        <item name="android:enforceStatusBarContrast">false</item>
        <item name="android:enforceNavigationBarContrast">false</item>
        <item name="android:windowLayoutInDisplayCutoutMode">shortEdges</item>
    </style>
</resources>
